<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/global.css" />
    <title>Document</title>
  </head>
  <body class="moviedetails">
    <h1>Movie Details</h1>
    <div class="onemovie">
      <h2>Title: <%= movie.title %></h2>
      <img src="/image/movies/<%= movie.slika %>" alt="" width="200" height="200">

      <% movie.sliki.forEach(slika => { %>
        <img src="/image/movies/<%= slika %>" alt="" width="200" height="200">
      <% }); %>
      
      <p>Year: <%= movie.year %></p>
      <h2>IMBD Rating: <%= movie.imdbRating %></h2>
    </div>
    <div class="img-container">
      <img src="" alt="">
    </div>
    <hr />
    <form action="/modifyMovie/<%= movie._id %>" method="POST">
      <label for="title">Title:</label>
      <input type="text" id="title" name="title" value="<%= movie.title %>" />
      <label for="year">Year:</label>
      <input type="number" id="year" name="year" value="<%= movie.year %>" />
      <label for="imdbRating">ImdbRating:</label>
      <input
        type="number"
        id="imdbRating"
        name="imdbRating"
        value="<%= movie.imdbRating %>"
      />
      <input type="file" name="slika">
      <button class="film-button" type="submit" name="createMovie">
      Edit
      </button>
    </form>
    <a href="/viewMovies">Go back</a>
  </body>
</html>